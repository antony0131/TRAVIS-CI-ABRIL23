language: csharp
mono: none
sudo: required
dist: focal
dotnet: 6.0.202
branches:
  only:
    - master
#    - develop
# Add the 'before_install' section to install Python 3.8
python:
  - 3.8
before_install:
  - pip install --upgrade pip
  - pip install --upgrade google-cloud-sdk
  - gcloud version
script:
  - dotnet build
  - dotnet test
# [START travis_deploy]
deploy:
    provider: gae
    # Skip cleanup so api_key.py and vendored dependencies are still there
    skip_cleanup: true
    keyfile: travis-net6-a23-c98189410a2b.json
    project: tests-402501
    version: 1.0.1
    default: true
    on:
        all_branches: true
# [END travis_deploy]